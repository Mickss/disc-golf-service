create table Events(
  id UUID,
  tournamentDate varchar(255),
  registrationStart varchar(255),
  registrationEnd varchar(255)
  pdga varchar(255),
  tournamentTitle varchar(255),
  region varchar(255),
  externalLink varchar(255),
);
